{
  "name": "internship-project-root",
  "private": true,
  "version": "0.0.0",
  "type": "module",
  "scripts": {
    "build": "cd frontend && npm install --no-audit --no-fund && npm run build"
  }
}
